PARIS Simon - OGL ISLDB monstars!

Cette procedure sert à avoir un format plus lisible que le .json. 

Placer le programme Python refactor.py dans le même répértoir que le resultat de notre essai (.json). Executer la commande suivant:

./refractor.py isldb.json | cat -n

permission non accrodée:
chmod -x ./refactor.py
